- name: Demo on conditions
  hosts: localhost
  vars: 
    OS: centos
    NUM: 7
  tasks:
    - name: centos 
      debug:
        msg: CentOS
      when: OS == "centos"
    - name: Ubuntu
      debug: 
        msg: Ubuntu
      when: OS == "ubuntu"