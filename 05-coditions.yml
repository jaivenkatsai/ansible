- name: Demo on conditions
  hosts: localhost
  vars: 
    OS: centos
  tasks:
    - name: centOS
      degug:
        msg: centOS
        when: OS == "Cento"
    - name: Ubuntu
      debug: 
        msg: Ubuntu
        when: OS == ubuntu